import 'package:flutter/material.dart';

class TodoAppHomeScreen extends StatelessWidget {
  const TodoAppHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Hello Bitch San!!'),
      ),
    );
  }
}